<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CustomWindow.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomWindow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CustomWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="0"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border
            x:Name="AppTitleBar"
            Grid.Row="0"
            VerticalAlignment="Top"
            x:FieldModifier="public"
            Visibility="Collapsed">
            <TextBlock
                x:Name="AppTitle"
                Text=""
                VerticalAlignment="Top"
                Margin="30, 8, 0, 0"/>
        </Border>

        <NavigationView x:Name="Navigation"
                        Grid.Row="1"
                        Loaded="Navigation_Loaded"
                        SelectionChanged="Navigation_SelectionChanged"
                        ItemInvoked="Navigation_ItemInvoked"
                        BackRequested="n_BackRequested"
                        IsBackButtonVisible="Auto">
            <NavigationView.MenuItems>
                <!-- Corrected namespace casing in Tag values -->
                <NavigationViewItem Icon="Setting" Content="프로그램 설정" Tag="CustomWindow.Pages.NormalSetting" />
                <NavigationViewItem Icon="Setting" Content="색 설정" Tag="CustomWindow.Pages.ColorSetting" />
                <NavigationViewItem Icon="Setting" Content="창 설정" Tag="CustomWindow.Pages.WindowSetting" />
            </NavigationView.MenuItems>

            <ScrollViewer>
                <Frame x:Name="ContentFrame"/>
            </ScrollViewer>
        </NavigationView>
    </Grid>
</Window>
