<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CustomWindow.Pages.ColorSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomWindow.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Stretch" MaxWidth="990">
        <ScrollViewer
            HorizontalAlignment="Stretch"
            IsDeferredScrollingEnabled="True"
            VerticalScrollBarVisibility="Visible"
            MaxWidth="990"
            Margin="0, 20, 0, 20">
            <StackPanel x:Name="Panel" HorizontalAlignment="Stretch" Spacing="8">

                <TextBlock Text="테두리 설정" Margin="0,0,0,8" FontSize="14" FontWeight="Bold"/>

                <controls:SettingsCard Header="테두리 색상" Description="현재 테두리 색상을 선택합니다." Margin="0,0,0,3">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE790;"/>
                    </controls:SettingsCard.HeaderIcon>
                    <StackPanel Orientation="Vertical" Spacing="8">
                        <ColorPicker
                            x:Name="BorderColorPicker"
                            IsAlphaEnabled="True"
                            Color="{x:Bind ViewModel.BorderColor, Mode=TwoWay}"
                            IsAlphaSliderVisible="True"
                            IsHexInputVisible="True"
                            ColorChanged="BorderColorPicker_ColorChanged"/>
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <TextBlock Text="HEX:" VerticalAlignment="Center"/>
                            <TextBox Width="120" Text="{x:Bind ViewModel.BorderColor, Mode=OneWay}" IsReadOnly="True"/>
                            <Border Width="24" Height="24" CornerRadius="4" BorderThickness="1" BorderBrush="#33000000" Background="{x:Bind ViewModel.BorderBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </controls:SettingsCard>

                <controls:SettingsCard Header="테두리 두께" Description="테두리 선의 두께를 조절합니다." Margin="0,0,0,3">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xED5E;"/>
                    </controls:SettingsCard.HeaderIcon>
                    <StackPanel Orientation="Vertical" Spacing="8">
                        <Slider Minimum="1" Maximum="20" StepFrequency="1" Value="{x:Bind ViewModel.BorderThickness, Mode=TwoWay}" ValueChanged="BorderThicknessSlider_ValueChanged"/>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="현재 두께:"/>
                            <TextBlock Text="{x:Bind ViewModel.BorderThickness, Mode=OneWay}" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </controls:SettingsCard>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>